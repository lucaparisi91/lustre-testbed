# Setting up all the VMs

We will be setting up two additional VMs :

- One combined MDS and MGS
- One OSS

The host machine will act the client.

# Generate the drives

```bash
sudo dnf install virt-manager
```